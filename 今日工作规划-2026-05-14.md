# 2026-05-14 工作规划

## 昨天完成的事（2026-05-13）

### 搜索链路全面诊断与修复
- **根因确认**：浏览器前端直连 DuckDuckGo / Wikipedia 时不稳定（跨站请求失败），但服务端 PowerShell 直连同一地址完全正常（返回 200）。
- **修复1 — 本地代理**：给 `launcher/serve-static.ps1` 加了 `/__roundtable_proxy` 同源代理接口；前端在本地运行时自动走代理，绕过浏览器跨站限制。
- **修复2 — 失败诊断可视化**：搜索失败不再只在控制台打日志，而是：
  - 状态栏直接显示哪个来源失败、失败原因（如"DuckDuckGo 失败：浏览器未能完成跨站请求"）。
  - 失败时也在证据区写入一条占位记录，让用户打开圆桌台就能看到失败详情，无需打开开发者工具。
- **修复3 — 成功状态改为诊断文案**：搜索成功后状态栏从固定"网页搜索结果已并入共享事实包"改为"已入库 X 条证据，来源状态：DuckDuckGo X 条；Wikipedia X 条"。
- **自动化回归**：新增 `scripts/validate-web-search-diagnostics.spec.cjs`，已通过 2 条用例（失败路径 + 成功路径），全程自动，无需手动看控制台。

---

## 明天要做的事

### 1. 补全"席位级搜索"的完整测试覆盖
- `runSpeakerWebSearch` 已经能在搜索失败时写占位证据，但还没有专门的自动化回归。
- 需要：在 `validate-web-search-diagnostics.spec.cjs` 里加一条"开始讨论后证据区出现席位级搜索占位记录"的用例，用 mock 模拟代理失败场景。

### 2. 把本地代理同步进 `dist` 打包流程
- `scripts/package-static.ps1` 打包时只复制文件，目前 `Start-Roundtable-Web.bat` 的 `dist` 版本使用的是 `dist/serve-static.ps1`，需要确认它也带了代理逻辑。
- 验证方式：跑一次 `npm run build`，然后用 `dist/serve-static.ps1` 起服务，打 `/__roundtable_proxy?kind=duck&q=test`，看是否返回正常结果。

### 3. 共享证据区的"失败占位记录"样式优化
- 当前失败记录和成功记录样式相同，用户需要读文字才能区分。
- 建议：加一个 `filterType: "web-fail"` 或 CSS class `evidence-list-item--failed`，让失败条目在列表里有明显视觉区分（如灰色 + 感叹号图标）。

### 4. 考虑加一个"重试搜索"按钮
- 失败占位证据详情区增加一个"重新搜索"操作，点击后对同一 query 重跑 `collectPublicWebResearch`，成功后替换占位记录。

### 5. 记忆系统下一步（按 2026-05-10 规划推进）
- 参考 `记忆系统规划-2026-05-10.md` 里的 Phase 2 计划：实现「共享事实」在讨论轮次间自动更新和持久化，解决目前每轮重启后记忆只依赖静态 brief 的问题。

---

## 今天已推送的文件清单
- `prototype-ui/app.js` — 核心修复（代理路由、诊断文案、失败证据写入）
- `launcher/serve-static.ps1` — 新增 `/__roundtable_proxy` 接口
- `scripts/validate-web-search-diagnostics.spec.cjs` — 新增自动化回归
- `package.json` — 新增 `@playwright/test` 开发依赖
